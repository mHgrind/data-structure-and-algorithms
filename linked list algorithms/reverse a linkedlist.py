# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
# ## "" SUR LEETCODE : 
""" class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        before = none
        temp = head


        while temp:
        after = temp.next
        temp.next = before
        before = temp
        temp = after

    return before










          """

# en temp normal sur une linkedlist dont on a access à la tail : 
""" 
def reverse(self):
    temp = self.head
    self.head = self.tail
    self.tail  temp
    after = temp.next
    before = None
    for _ in range(self.length):
    after = temp.next
    temp.next = before
    before = temp








"""